# lfd
Learnning from demonstration (lfd) packages supporting robotic kinesthetic learning from demonstration.


## Running scripts

Source a virtual python environment.
Change to root directory of project.

For each example:

    $ python -m scripts.gmm_keyframe_example
    $ python -m scripts.kmeans_clustering_example
